const num1 = parseInt(prompt("Primeiro número: "))
const num2 = parseInt(prompt("Segundo número: "))


const soma = (n1, n2) => {

    const soma = n1 + n2
    alert(soma)
    
}

soma(num1, num2)